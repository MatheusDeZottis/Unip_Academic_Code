{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compilar e Executar C",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "gcc '${file}' -o '${fileDirname}/${fileBasenameNoExtension}' && '${fileDirname}/${fileBasenameNoExtension}'"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Compila e executa código C usando GCC no Ubuntu"
    }
  ]
}
